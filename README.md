# HR Performance Evaluator

This repository contains a simple command line application for evaluating employee performance from a CSV file.

## Usage

1. Prepare a CSV file with columns `name` and `score` (0-100).
2. Run the script:

```bash
python hr_eval.py employees.csv
```

The program will output overall statistics and a categorized list of employee performance.
